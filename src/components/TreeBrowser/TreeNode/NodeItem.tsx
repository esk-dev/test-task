import React, { useState, useEffect } from "react";
import { ITreeNode } from "../../../interfaces/index";

type Props = {
  nodes?: Array<ITreeNode>;
  level: number;
};

function NodeItem({ nodes, level }: Props) {
    
}